const Category = (data: any) => {
  return (
    <>
      {/* <div>
                {data.data?.map((product:any) => (
                    <div key={product.id}>
                        <h1>{product.Name}</h1>
                        <h1>{product.Price}</h1>
                    </div>
                ))}
            </div> */}
    </>
  )
}

export default Category
